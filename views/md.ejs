<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/md.css">
    <title>Twice</title>
</head>
<body id= mainbackground>
    <%- include ('partials/navbar.ejs') %>
    <div class="container-fluid mt-3">
        <div id=h1 class="text-center"><h2>Membership Database</h2></div>
        <form action ="/md" method="POST" enctype="multipart/form-data"></form>
    <table class="table table-hover small text-center">
      <thead>
          <tr>
            <th width="100">Member Name</th>
            <th width="100">Member Image</th>
            <th width="50">Location</th>
            <th width="50">Favourite Idol</th>
            <th width="50">Favourite Song</th>
            <th width="50">Edit</th>
            <th width="50">Delete</th>
          </tr>
      </thead>
      <tbody>
        <% for(let i = 0; i < twicefanmembership.length; i++) { %>
          <tr>
            <td><a href="/membership/<%= twicefanmembership[i].memberId %>" ><%= twicefanmembership[i].membername %></a></td>
            <td><img src = "images/<%= twicefanmembership[i].memberimage %>" width= "30%"></td>
            <td><%= twicefanmembership[i].location %></td>
            <td><%= twicefanmembership[i].favouriteidol %></td>
            <td><%= twicefanmembership[i].favouritesong %></td>
            <!-- Edit Link -->
            <td><a href="/updateMembership/<%= twicefanmembership[i].memberId %>">Edit</a></td>
            <!-- Delete Link -->
            <td><a href="/deleteMembership/<%= twicefanmembership[i].memberId %>" onclick="return confirm('Are you sure you want to delete this product?')">Delete</a></td>
          </tr>
        <% } %>
      </tbody>
    </table>
    </div>
</body>
</html>